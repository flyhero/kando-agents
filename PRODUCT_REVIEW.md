# Kando 产品评价与改进建议

评估日期：2026-09-25

## 总体评价

Kando 已经是一个有明确价值、界面完成度不错的开发者工具，但“把工作交给 agent”比“掌握进度、验收成果”做得更完整。下一阶段最值得投入的是状态反馈和结果验收。

本次评估查看了实际运行的任务详情、自由会话界面，以及主要前后端实现。以下评分是产品体验判断，不是用户测试结果。

| 维度 | 评价 | 核心判断 |
| --- | --- | --- |
| 交互性 | 7/10 | 常用操作简洁，但操作含义和运行状态不够直观 |
| 美观 | 8/10 | 克制、统一、适合长时间使用，布局仍有优化空间 |
| 功能性 | 7.5/10 | 任务准备和执行丰富，验收闭环明显不足 |
| 实用性 | 熟练开发者 8/10；新用户 6/10 | 能解决真实痛点，但需要用户理解终端、分支和后台服务 |

## 1. 交互性：轻量是优势，反馈不足是主要短板

做得好的地方包括：只填标题就能创建任务；图片支持粘贴和拖入；无法执行时能解释原因；agent 提交的方案需要用户接受，并支持撤销。这些设计符合实际工作习惯。

优先改进三个地方：

- **让主要操作有文字。** 任务顶部的细化、执行、完成主要靠图标辨认。建议把当前最重要的操作显示为“开始执行”“继续执行”这样的文字按钮，次要操作保留图标。
- **让用户一眼找到需要自己处理的内容。** 增加“等待输入”“等待审批”“待验收”提示，以及“需要我处理”的聚合入口。管理多个 agent 时，这比知道几个进程还活着更有用。
- **明确保存状态。** 编辑器已有自动保存，但没有“保存中／已保存／保存失败”的反馈。代码中还存在可靠性隐患：请求成功前就更新本地 `saved` 标记，失败后没有看到自动重试，可能出现文字仍在界面上、实际没有持久化的情况。这是代码审查发现，尚未通过断网复现。参见 [MarkdownEditor.tsx](packages/desktop/src/renderer/src/components/MarkdownEditor.tsx) 中的 `flush` 实现。

另一个容易忽略的问题：在“只显示未执行和执行中”的筛选下，执行结束的任务会从列表消失。建议让待验收任务继续可见。

## 2. 美观：方向是对的，建议精修信息层级

实际浅色界面的暖灰侧栏、白色内容区、细边框和少量强调色比较协调。任务正文与右侧属性分开，阅读清晰；终端也没有过多装饰。代码中还考虑了深色主题和窄窗口布局。

目前主要是空间分配和重点不够突出：

- 左侧任务、会话默认各占一半高度，即使数量差异很大也如此。虽然可以折叠，仍建议支持拖动分界线并记住比例。
- 侧栏固定 320px，长标题容易换行和截断。建议允许调宽、整体收起，并提供完整标题提示。
- 顶部操作视觉权重接近，执行按钮不够突出；删除、停止等操作应与日常导航拉开距离。
- 右侧属性较少时留白较多，可以支持收起属性区，把空间留给正文或终端。

审美建议是：保留现在安静、偏原生工具的风格，通过按钮主次、文字层级和可调布局提升品质。

## 3. 功能性：最需要补的是“执行结束以后”

现有功能已经有实际深度：多仓库 worktree、任务依赖、只读细化、方案确认、继续与重做、跨 agent 移交、Jira 收件箱、图片和终端回放。尤其是关闭窗口不影响 agent、保留 worktree，符合开发工具应有的可靠性要求。

但有一个优先级很高的状态问题：

当前任务在会话退出后统一进入 `done`，这个处理没有接收退出码；依赖判断又把 `done` 视为可继续执行的条件。因此，异常退出的前置任务也可能解除后续任务的执行限制。界面叫“已执行”虽然比较谨慎，但它仍不能证明结果可用。

相关实现：

- [task-service.ts](packages/core/src/task-service.ts)：`handleSessionExit` 和 `reconcile` 中的任务状态更新。
- [task.ts](packages/protocol/src/task.ts)：`checkRun` 中的依赖判断。

建议把三件事分开表达：

- 会话状态：工作中、等待输入、已退出。
- 执行结果：正常退出、失败、中断、未知。
- 验收状态：待验收、已接受、需要修改。

正常退出也不能自动等于任务完成。依赖是否放行，最好有明确的验收策略。

随后补一个最小结果页：改了哪些文件、diff、测试结果及其来源、所在分支、下一步操作。先支持查看变更和打开 IDE，再完善合并与显式清理。当前 [README](README.md#下一步) 也把验收流程列为待办，这个优先级值得提前。

## 4. 实用性：适合个人多任务开发，规模扩大后的管理能力还不足

如果日常同时处理多个仓库、使用两种 agent、从 Jira 接任务，Kando 已经有明确用途：减少来回切换终端、寻找上下文和维护工作目录的负担。

任务积累之后，瓶颈会转向“找不到”和“看不过来”：

- **搜索与组织。** 任务列表目前主要是全部／未结束两种筛选，建议补标题与 issue key 搜索、项目筛选、置顶和归档。自由会话已有分组排序，可以统一两边的操作方式。
- **会话标题。** 当前会话标题采用首条用户消息的首行截取，实际界面中已经出现不表达业务含义的标题。建议提供重新命名或可选摘要标题，并保留手动名称。参见 [conversation-service.ts](packages/core/src/conversation-service.ts) 中的自动标题逻辑。
- **首次使用。** 面向其他用户分发时，需要安装包、后台服务自动启动，以及 agent 可用性检查。“等待 core 启动”适合开发调试，对首次使用者缺少可直接执行的解决办法。
- **更大规模并行。** 再考虑并发上限、执行队列，以及自由会话同时修改同一目录的提示。

## 建议的推进顺序

| 优先级 | 工作 | 预期收益 |
| --- | --- | --- |
| P0 | 区分退出结果、验收状态和依赖放行条件 | 避免把失败结果当成可用前置条件 |
| P0 | 保存确认、失败重试和草稿保留 | 提高编辑可靠性 |
| P1 | 等待输入／审批提醒、待处理聚合入口 | 降低多 agent 管理成本 |
| P1 | 结果页、diff、测试结果、打开 IDE | 补齐任务交付闭环 |
| P2 | 搜索筛选、文字主按钮、可调布局 | 提高日常操作效率 |
| 对外分发前 | 打包、自启动、首次使用引导 | 降低使用门槛 |

## 评估与验证范围

本次评价未修改产品代码。评估时运行了 `pnpm typecheck` 和 `pnpm test`，类型检查通过，31 个测试文件中的全部 166 项测试通过。

测试结果支持现有工程基础，但不代替上述交互和异常场景验证。视觉评价主要依据实际浅色界面；深色主题和窄窗口适配通过代码确认，未进行完整的视觉验收。
